apiVersion: v1
kind: Service
metadata:
  name: tuva-empi-backend-dev
spec:
  type: NodePort
  selector:
    app: tuva-empi-backend-dev
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080
